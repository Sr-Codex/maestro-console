# BMAD — saídas geradas pelos agentes
_bmad-output/

# BMAD — config pessoal (não versionar)
_bmad/config.user.toml

# Node
node_modules/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# maestro — estado de runtime (SQLite/logs)
maestro/state/*.db
maestro/state/logs/

# spike — código descartável (benchmarks/protótipos; não faz parte do produto)
spike/

# ADRs — Decisões arquiteturais (maestro console)

> Fonte da verdade dos ADRs (versionada no git). Antes ficavam em
> `_bmad-output/planning-artifacts/architecture.md` §9, que é **ignorado pelo git** do repo
> (todo o `_bmad-output/` está no `.gitignore`); movidos para cá em 2026-06-26 para terem
> histórico/versionamento. Formato curto conforme a regra global de "stack durável"
> (decisão + alternativas + verificação + data). **ADR é imutável** — mudança = **novo ADR**.

- **ADR-1:** Headless como caminho de dados (vs. raspar TUI). *Motivo:* 100% confiável, sem ANSI/eco. *Trade-off:* TUI vira só visibilidade.
- **ADR-2:** Orquestrador-mediado (vs. agente↔agente direto estilo Maestri). *Motivo:* robustez, controle, escalonamento.
- **ADR-3:** Python + asyncio no MVP (vs. Go/Rust). *Motivo:* velocidade de prototipagem, validado no spike; reavaliar core depois.
- **ADR-4:** Perfis TOML declarativos (vs. código por agente). *Motivo:* extensibilidade sem tocar no core.
- **ADR-5:** Sessões persistentes via `--resume` (vs. stateless ou prompt gigante). *Motivo:* contexto multi-turno barato; validado.
- **ADR-6 (segurança) [revisado E2-S2]:** Confinamento real via **bwrap** (workspace rw, `/tmp` privado tmpfs, resto read-only, rede mantida, config/sessão do agente rw), **fail-safe** se ausente. As flags de CLI (`--permission-mode acceptEdits` / `--sandbox workspace-write`) só evitam prompts — **não** confinam (teste real provou que `acceptEdits` sozinho escreve em `/tmp`; o limite efetivo é o bwrap). Sem `--dangerously-*`. *Motivo:* isolamento é fundação (multi-agente + publicação); flags de CLI dariam garantia falsa.
- **ADR-7 (protocolo):** Envelope em **JSON estrito validado por schema** (vs. texto tolerante). Campos `message_id`/`task_id`, limite em bytes, **rejeição de formato inválido**. *Motivo:* falhar alto > corromper silenciosamente; rastreabilidade/idempotência.
- **ADR-8 (concorrência):** **Mutex por `agent_id`/`session_id`** — 1 tarefa ativa por sessão; concorrência só entre agentes distintos. *Motivo:* prompts paralelos na mesma sessão corrompem contexto.
- **ADR-9 (persistência):** **SQLite (WAL)** (ou JSON atômico com escritor único). *Motivo:* escrita JSON concorrente direta corrompe estado; SQLite dá transações/locking.
- **ADR-10 (interop/bridge — futuro, condicional) [2026-06-25]:** Avaliado *driving* de CLIs interativas (raspar PTY/TUI) para uma eventual **interop multi-ferramenta** (estilo Maestri: Claude↔Codex↔OpenCode entre terminais). **Decisão:** o caminho de dados **permanece headless + orquestrador-mediado (reafirma ADR-1 e ADR-2)** — raspar PTY **não** é caminho de dados confiável nem porta o Envelope JSON. *Se* a interop com agentes externos que só expõem TUI for adotada, será um **adapter OPCIONAL e ISOLADO** (Camada Anticorrupção — regra 5 de stack durável), tratado como bridge *best-effort*/visibilidade, com **detecção de quiescência explícita** (sentinela), nunca como protocolo confiável.
  - **Mecanismo escolhido (se/quando):** **tmux** (`send-keys` + `capture-pane`) — robusto/battle-tested para TUIs full-screen.
  - **Alternativas rejeitadas:** **pexpect** (ANSI de TUI quebra o rendering — partial/broken); **tmux-bridge** (é *human-in-the-loop* p/ sudo/auth, não turno autônomo); **agente↔agente direto como caminho primário** (já rejeitado no ADR-2; reforçado pelo *echoing* — colapso de identidade multi-agente, arXiv 2511.09710).
  - **Risco registrado:** a interop heterogênea da Maestri é **afirmada mas não testada** (demos só Claude↔Claude) — não tratar como pronta.
  - **Longevidade verificada (consulta 2026-06-25):** *tmux* — ativo, maduro, battle-tested. *pexpect 4.9.0* (lançado 25/nov/2023; major anterior 4.8.0 em jan/2020 → cadência lenta ~3–4 anos; Production/Stable; depende de `ptyprocess>=0.5`). Nenhum é dependência do core hoje. *Motivo:* falhar alto > corromper silenciosamente; isolar o volátil; não reabrir fundação sem necessidade comprovada.
- **ADR-11 (cabos interativos — skill `ask` iniciada pelo agente, mediada pelo host) [2026-06-25]:** Para reproduzir o "cabo que conversa" do Maestri SEM abrir mão da robustez, adota-se um modo **interativo iniciado pelo agente e mediado pelo host** (variante "a" da pesquisa): ligar o cabo A→B instala uma **skill de CLI agnóstica** (`maestro-ask B "<prompt>"`) que o agente A chama de dentro do seu terminal ao vivo; o host roteia via **`controller.delegate` (headless + bwrap + Envelope)** e devolve a resposta. **Decisão:** isto **NÃO é raspagem de PTY** (distinto do ADR-10) e **mantém o caminho de dados mediado** (reafirma ADR-1/2/6/7) — a única novidade é a *iniciativa* do handoff passar do humano/orquestrador para o **agente**, com guardrails. A variante "raspar o terminal vivo de B" (visibilidade ambos-ao-vivo) fica **adiada**; quando feita, recai no ADR-10 (bridge isolado/best-effort).
  - **Transporte:** *mailbox* de arquivos (req/resp JSON) num dir montado rw via o `shared_paths` **já existente** do bwrap — vs Unix socket. *Motivo:* reusa a montagem de sandbox atual, zero plumbing novo, stdlib.
  - **Identidade do remetente:** `--setenv MAESTRO_NODE <nó>` no bwrap; o cliente lê do env.
  - **Sessão do destino (MVP):** sessão **efêmera** no `delegate` do ask (vs resumir a sessão interativa de B) — evita contenção com o mutex por-sessão do terminal vivo (ADR-8); reavaliar "responder em contexto" depois.
  - **Guardrails obrigatórios** (pesquisa: *echoing* 5–70% a partir do turno ~7, arXiv 2511.09710): **limite de turnos por cabo**, **refresh de identidade (~3 turnos)**, **anti-loop A↔B (teto de profundidade)**, teto de bytes (reusa o limite do Envelope), e input do outro agente tratado como **não-confiável** (já roda em bwrap).
  - **Stack/longevidade (verificada 2026-06-25):** reusa a stack atual (Python + GTK4/VTE 0.84, mantida); mailbox = stdlib (sem dependência nova no MVP). Detalhes em `docs/05-pesquisa-cabos-interativos.md`.
- **ADR-12 (canvas infinito — modelo câmera + container) [2026-06-26]:** O canvas nativo usa **modelo de câmera** (`tela = base·zoom + cam`; sem parede), com os filhos posicionados por `Gtk.Fixed.set_child_transform` e a câmera (`self._cam`) movida no pan. **Container = `Gtk.ScrolledWindow` em policy `EXTERNAL`** (NÃO `NEVER`). *Motivo:* com `NEVER` o ScrolledWindow não rola e por isso exige o **mínimo INTEIRO do filho**; como o `Gtk.Fixed` mede a caixa dos filhos incluindo o transform (câmera assada), o mínimo crescia ao panar e **empurrava o toplevel** (janela inchava/saía da tela; maximizar quebrava). `EXTERNAL` rola programaticamente (sem barra) e **recorta na viewport** → a câmera dá alcance infinito sem crescer a janela. Complemento: `Viewport.set_scroll_to_focus(False)` (evita deslize ao focar um filho). **Validado em runtime no CM4** (5+ min de pan/zoom, sem OOM de GPU) — **reverte o "adiado pro CM5"** (a inviabilidade anterior era GPU contaminada por crash prévio, não limite real). Ver `docs/11-pesquisa-canvas-infinito-gpu.md`.
  - **Lição (rejeitado):** sobrescrever `do_measure` numa subclasse de `Gtk.Fixed` p/ capar o tamanho **NÃO funciona** (é ignorado — `GtkFixedLayout` segue medindo os filhos); a alavanca correta é a **policy do ScrolledWindow**.
  - **Entrada de pan:** no uConsole, **SELECT + trackball gera eventos de scroll** (firmware/X11, ver `docs/uconsole.md`/INVENTARIO); um `Gtk.EventControllerScroll` traduz os deltas em movimento de câmera (pan suave, sem clicar).
  - **Seleção + roteamento de scroll:** clicar seleciona nó/nota/árvore (borda azul tracejada via `outline`); o controller de scroll roda na **fase CAPTURE** → o pan é interceptado antes do VTE e **nunca é roubado** ao passar sobre uma janela; o scroll só "entra" no elemento **selecionado e sob o cursor** (ex.: scrollback do terminal).
  - **Método de diagnóstico (registro):** a causa-raiz só fechou com **medição ao vivo** (`xprop`/`xwininfo` + log de `measure()` por frame). Teoria, percepção, um workflow multi-agente e teste via `xdotool` deram **falsos resultados** — lição: medir o estado real cedo.
- **ADR-13 (cabo modo LIVE — injeção no terminal vivo, variante b implementada) [2026-06-27]:** Implementado o modo **live** do cabo (estilo Maestri), **revertendo o "adiado"** das variantes (b) do ADR-10/ADR-11. Ao perguntar por um cabo (`maestro-ask`), o host **digita o prompt no terminal VIVO do agente destino** (`Vte.feed_child`), **monitora** esse terminal (quiescência + sumiço do "esc to interrupt"), **só quando DESFOCADO** (igual Maestri), e devolve a resposta. **Default = live**, com **fallback headless** automático (variante a / ADR-11) se a captura falhar — quem pergunta sempre recebe algo; `MAESTRO_ASK_MODE=headless` força o mediado. **Decisão:** a variante (b), antes recusada por fragilidade, é viável **desde que** (i) seja best-effort com fallback, (ii) o caminho headless/Envelope continue existindo (não reabrir ADR-1/2). `ask_bus`/`ask_router`/cliente inalterados; só `_ask_delegate` muda. Heurística de captura pura/testável em `maestro/native/ask_capture.py`.
  - **Lições de injeção em TUI via PTY (verificadas em runtime + pesquisa):** (1) **Enter = transmissão SEPARADA** do texto, ~300ms depois — `texto+Enter` na mesma rajada é tratado como **paste** (Enter vira newline e NÃO envia). (2) Enter = **`\r` (C-m)**, não `\n` (em raw-mode `\n` insere quebra; no claude Enter envia / Ctrl+J quebra). (3) `Vte.feed_child` espera **bytes** no VTE 3.91. (4) **Colapsar o prompt em 1 linha** (frame multi-linha quebra a caixa). (5) VTE só na **main thread** (worker faz marshalling via `idle_add` + `Event`). (6) Captura de TUI full-screen é **frágil (~70%, nível Maestri)** → fallback é parte do design. Fontes: automações tmux+claude-code, [claude-code#15553], [xterm bracketed-paste], + probe em VTE real. Imutável (mudança = novo ADR).
- **ADR-14 (física do cabo — Verlet rope, 3 modos comutáveis) [2026-06-29]:** O cabo do canvas deixou de ser um bezier estático e passou a ter **física**. Adotada a **corda Verlet** (cadeia de pontos + restrições de distância + gravidade — Jakobsen, *Advanced Character Physics*, GDC 2001) como **default**, com **catenária (sag estático)** e **bezier+mola** como modos alternativos — o usuário cicla com **Ctrl+Shift+P** (gosto pessoal; default Verlet). **Decisão:** manter os 3 (não escolher um só) porque o custo é baixo — todos geram uma lista de pontos desenhada igual (spline Catmull-Rom, bolinha nas pontas, tracejado de fluxo por cima). **Pontas no ímã de 8 pontos** (4 meios de borda + 4 cantos, par mais próximo); a **troca de âncora é suavizada** (o endpoint escorrega, não teleporta — evita o tranco). **Bateria (CM4):** simula num `add_tick_callback` (frame clock GTK4) que **dorme ~0,5 s após assentar** — canvas parado = sem tick; passo de tempo **fixo** (estável p/ Verlet). Núcleo **puro/testável** em `maestro/native/rope.py` (sem GTK; `tests/test_native_rope.py`), desenho/integração em `canvas.py`. *Alternativas:* só catenária (rejeitada como única — sem inércia/balanço); física por engine externa (rejeitada — overkill no ARM). Pesquisa multi-fonte: ver `docs/12-pesquisa-cabo-fisica.md`. Imutável (mudança = novo ADR).
- **ADR-15 (editor de notas — markdown in-place NATIVO; rejeitados GtkSourceView e WebKit) [2026-06-29]:** O editor das notas do canvas usa **Gtk.TextView nativo (GTK4)** com **estilo markdown AO VIVO** (regex → `Gtk.TextTag` no `changed`, marcadores visíveis) ao editar, e **render limpo num `Gtk.Label` (markup Pango) ao perder o foco** (marcadores somem). Botões de formatação **alternam** (`md_wrap_toggle`). Núcleo puro/testável em `engine/notes.py` (`md_spans`/`md_to_pango`/`md_wrap_toggle`; `tests/test_notes.py`). **Decisão por cardápio de stack (básica/média/avançada apresentado ao usuário):** **(a)** *Avançada — WebKitGTK 6.0 + Toast UI (WYSIWYG real, esconde marcador):* **instalada e validada num spike** (ponte JS↔Python OK, ~525MB/instância) e **REJEITADA** — embutir o WebView sobre o card pequeno num canvas de pan/zoom ficou espremido, bloqueava o pan e pesado; o painel lateral (Heptabase) funcionava mas o usuário preferiu editar in-place. **(b)** *Média — GtkSourceView 5:* **instalada e REJEITADA** — a `markdown.lang` é básica (1 nível de header, sem tachado), não entrega H1/H2/H3 distintos e briga com a cor pastel da sticky. **(c)** *Básica — TextView + TextTags (escolhida):* leve, nativa (sem dep APT), compõe com a sticky, e dá controle total (H1/H2/H3, tachado, toggle, estilo ao vivo). **Ponto sensível registrado:** o "formata ao sair" é um **toggle por foco** (edit↔view) — fragilidade conhecida, tratada nos botões que mexem no texto; se a nota virar documento longo, reavaliar superfície-única ou GtkSourceView. *Lição de processo aplicada:* stack vira **cardápio em camadas pro usuário escolher**, não pré-decisão pela mais leve (o usuário cobrou após 2 trocas; ver `~/.claude/LESSONS.md`). Pacotes do spike (GtkSource 5, WebKit 6 typelibs) **removidos** — ver `UCONSOLE_CHANGES.md`. Fontes: Apostrophe (GtkSourceView+custom, ref. GNOME), Marko Editor (WYSIWYG-no-TextView, alpha — prova que esconder marcador ao vivo é difícil), Heptabase/Miro Cards (painel p/ card rico), [TwiRp/gtk4-textview-grammar] + [hardcoeur/markdownview] (regex+TextTags é padrão real). Imutável (mudança = novo ADR).
- **ADR-16 (Maestro mode — extensão ORIGINAL, não-fiel ao Maestri; autoridade no host) [2026-06-30]:** O "Maestro mode" (um agente vira **manager** e **recruta/conecta/dispensa** outros agentes via o shim `maestri`, ask-bus estendido com `cmd`/`args`) é uma **feature original deste projeto, NÃO uma reprodução do Maestri.** Pesquisa ao vivo (30/06/2026; site oficial `themaestri.app` + 4 fontes independentes: Product Hunt, agent-finder.co, ProductCool) confirmou que **no Maestri real a orquestração é dirigida pelo humano** — você cria os nós e desenha os cabos PTF; o agente **não** cria/recruta agentes ("There is no concept of agents creating, recruiting, or managing other agents"). O que parece "sub-agent" lá são **branches/worktrees de git** (Floors), não agente-cria-agente. **Decisão:** adotar a feature como **divergência consciente** (diferencial do clone), registrada aqui — *correção de conduta:* deveria ter sido sinalizada ao usuário ANTES de implementar (a Fase 6 foi construída sem validar que existia no original; o usuário concordou em mantê-la como extensão). 
  - **Modelo de segurança (princípio que rege a feature):** segurança multi-agente **não é composicional** — assuma que qualquer agente pode ser sequestrado por conteúdo (prompt injection / *confused deputy*). Logo, **toda autoridade — spawn, wire, profundidade, budget — é imposta pelo HOST a partir de estado que SÓ o host controla; nunca derivada de campos que o agente preenche** (`frm`, `depth`). Pesquisa: CSA/NIST AI RMF Agentic Profile (mar/2026); OWASP Top 10 for Agentic Apps 2026 (ASI01 Goal Hijack, ASI07 Insecure Inter-Agent Comms, ASI08 Cascading Failures); Triedman et al. arXiv:2510.17276 (2025); "Can AI Agents Have Babies?" IJRSI 2026 (spawn emergente com shell access); BAMAS arXiv:2511.21572 (budget agregado). Consultas em 30/06/2026.
  - **4 controles obrigatórios no host** (do mais crítico): (1) **Identidade por canal** — cada agente só enxerga a PRÓPRIA caixa de correio (bind-mount RW isolado, `<bus>/box/<nó>/`; shims num `<bus>/bin` RO); o host deriva o `frm` do dono da pasta e **ignora** o `frm` do payload → spoofing impossível por construção. (2) **Tetos no host a partir da linhagem real** — teto global de fleet + profundidade derivada da árvore + per-manager (mantém 6) + rate-limit de spawn; recrutas nascem **sem** poder recrutar (promover exige toggle humano). (3) **Kill-switch global** (cápsula principal) + **budget** (soft cap pausa-e-confirma; hard cap). (4) **Trilha de auditoria** append-only (linhagem + gatilho + custo) + **HUD** do fleet.
  - **Identidade por canal — decisão de stack (cardápio básica/média/avançada apresentado ao usuário; escolhida a "avançada"):** **Opção 1 (escolhida) — caixa por agente (file-mailbox por-nó):** reusa o transporte de arquivos atual (ADR-11), obra cirúrgica e de baixo risco, mata o spoofing pelo canal. *Rejeitada para agora:* **token secreto por agente** (proteção por convenção, não por construção — token vaza se o agente for sequestrado).
  - **Opção 3 — socket Unix por agente (EVOLUÇÃO FUTURA registrada, gatilho explícito):** trocar o file-mailbox por um **Unix domain socket bind-mounted por agente** + `GSocketService` no loop GLib. **Não melhora a segurança** (a identidade-por-canal da Opção 1 já é máxima), mas **habilita capacidades novas** — o *diferencial*: (a) **push/tempo real** sem atraso de polling; (b) **streaming ao vivo** da saída de um agente para outro (cabos "vivos" token-a-token, como os cabos PTY do Maestri original); (c) **backpressure** nativo do kernel; (d) **detecção instantânea de queda** (a conexão cai quando o agente morre → reaping de zumbis imediato). **Gatilho para puxar a alavanca:** quando/se quisermos cabos que transmitam **fluxo ao vivo** (não só pedidos esporádicos). Padrão canônico do ecossistema bwrap/Flatpak; é a migração "natural" do transporte. **Isolar o volátil:** a Etapa 1 põe o transporte atrás de uma **interface fina** (regra 5 de stack durável) para que essa troca, no futuro, não se espalhe pelo código. Fonte IPC: pesquisa multi-fonte 30/06/2026 (ArchWiki Bubblewrap; MCP transports spec 2025-06-18 — stdio para agente local; MCP-vs-A2A — recruit é MCP-shaped, não A2A; maildir/rename atômico).
  - **Verificação no código (30/06/2026):** confirmado que cada agente já tem **workspace próprio + bwrap próprio** (`agents.py` `agent_argv` + `Workspace.create(nid)`) → o "blast radius do fleet inteiro" do relatório de segurança **não se aplica** (workspaces isolados); o gap real é o **mailbox compartilhado + `frm` confiável** (corrigido pela Opção 1) e a **ausência de tetos/kill-switch/budget/auditoria impostos pelo host**. Imutável (mudança = novo ADR).
- **ADR-17 (Maestro mode seguro — identidade por SOCKET por agente + sequência revisada; supersede o transporte do ADR-16) [2026-06-30]:** Após **2ª rodada de pesquisa adversarial sobre o próprio plano** (dois revisores: desenho técnico + completude/conduta; fontes 30/06/2026), revisa-se o ADR-16. **Decisão de transporte mudou:** a identidade-por-canal será feita **direto via Unix domain socket *pathname* por agente** (a "Opção 3" do ADR-16 **vira a fundação**, não evolução futura) — decisão do usuário ("ir direto pra Op 3"). *Motivo:* o spool de **arquivos** (Opção 1) exigia blindar uma classe inteira de ataques de filesystem (symlink no `resp` = **escrita arbitrária no host**; TOCTOU; DoS de inode; `O_NOFOLLOW`/`openat2`; cota; `GFileMonitor`) — que o **socket elimina nativamente** (kernel-backed, sem caminho a seguir, framing nativo, backpressure, detecção de queda). Como a Op 3 viria logo, fazer arquivos-blindados primeiro seria **construir-e-descartar**. As Etapas 2–4 (abaixo) são **agnósticas de transporte** e assentam igual.
  - **Desenho do socket (invariantes de segurança):** o host cria **um listener por agente** em `<bus>/box/<nó>/sock` (**pathname socket** dentro da box RW bind-montada **só** naquele agente). **OBRIGATÓRIO pathname, NÃO abstract socket** — abstract vive no *network namespace* (compartilhado pelos agentes, que têm rede) → seria visível a todos; pathname vive no *filesystem* → o bind-mount o isola. **Identidade = qual listener aceitou a conexão** (kernel-backed, infalsificável); o host carimba `frm=<nó>` e **ignora** qualquer `frm` do payload. **Invariante (gravar e nunca quebrar em refactor):** a segurança vem da **ausência do mount** das boxes irmãs (não da flag `ro`) — *nunca* montar o diretório-pai que contém as boxes. Precedente: Flatpak document portal usa ID infalsificável do sistema, não o que o app informa (pós-CVE-2021-41133). bwrap dos agentes roda **não-root + `--cap-drop ALL`** (fecha o remount-rw do bubblewrap#287; o kernel trava `MS_RDONLY` em userns menos privilegiado). Box pré-criada pelo host `0700` antes do spawn; `<id>`/args validados por regex antes de qualquer uso. **Interface fina de transporte** (regra 5 — isolar o volátil): host-loop e shims falam por um seam (`Transport.serve`/`send`) para o transporte não vazar pelo código.
  - **Sequência REVISADA (a ordem do ADR-16 tinha 2 furos — kill-switch e auditoria estavam tarde):** **Etapa 1 (fundação):** identidade por socket + **log append-only desde o 1º evento**. **Etapa 2 (backstop ANTES de ligar spawn):** **kill-switch global que CEIFA a subárvore inteira** (reusa a árvore de pidfd do hardening da Fase 5/ADR — matar só o gerente é teatro) + hard-cap global; **provado em tabletop drill** (a literatura: o drill é a única prova). **Etapa 3:** tetos por **linhagem real** (global + profundidade derivada da árvore + per-manager 6 + rate-limit token-bucket) + recruta nasce **sem** recrutar + **budget por CUSTO REAL de tokens** (contagem de agentes é proxy ruim — teatro parcial) + **HITL por irreversibilidade** (ações não-reversíveis exigem aprovação humana, não só promoção/budget). **Etapa 4:** HUD + **detecção de anomalia ATIVA** (gatilho → kill-switch automático; trail/HUD passivos são o elo fraco) + **detecção de ciclo na linhagem** (profundidade não impede A↔B ping-pong).
  - **Risco residual aceito (adiado, registrado — escopo "Etapas 1–4" escolhido pelo usuário):** **proveniência/tainting pleno do conteúdo** (ASI06/ASI07 — identidade diz *quem*, não *o quê*), **validação semântica/intent plena** (além da allow-list de comando — ASI01), e **egress allow-list de rede** (hoje `sandbox.wrap` roda `allow_network=True` sem `--unshare-net` → agentes têm internet plena; é risco **pré-existente de TODO o app**, não só do Maestro mode, pois os agentes são CLIs de nuvem e precisam da API → isolar exige proxy de egress). Controles compensatórios no ínterim: caps + kill-switch + HITL-irreversível + auditoria.
  - **Mapa de cobertura (OWASP Top 10 for Agentic Apps 2026):** mitiga forte **ASI03** (identity/privilege — socket), **ASI07** (inter-agent comm), **ASI08** (cascading — caps+ciclo), **ASI10** (rogue agents — least-agency, recruta nasce sem recrutar), **ASI09** (human-agent trust — HITL irreversível); **parcial/adiado:** **ASI06** (context poisoning — proveniência), **ASI01** (goal hijack — semântica plena). Conduta: **flag default-OFF** até passar em **red-team adversarial por etapa** (forjar `frm`, estourar teto, escapar do kill-switch); threat model CSA MAESTRO / OWASP MAS Threat Modeling Guide v1.0. **Fontes (30/06/2026):** Flatpak GHSA-67h7-w3jq-vh4q + portal (blogs.gnome.org 2018); unix(7) SO_PEERCRED; bubblewrap#287; mount_namespaces(7); Sonar/openat2 TOCTOU; OWASP Top 10 Agentic Apps 2026 + MAS Threat Modeling Guide v1.0 (abr/2025); CSA MAESTRO (fev/2025); CSA Agentic NIST AI RMF Profile (mar/2026); SentinelAgent arXiv:2604.02767; Galileo/LangGraph incident write-ups. Imutável (mudança = novo ADR).
- **ADR-18 (autoridade de comando = LINHAGEM, não cabos; fecha confused-deputy) [2026-06-30]:** Revisão adversarial pós-implementação (3 revisores lendo o código) achou que `dismiss`/`reassign`/`wire` autorizavam pela função `_maestro_connected` (= os **cabos/edges**), mas `wire` deixa o agente **criar cabos** → **confused deputy**: um manager sequestrado faz `wire <vítima>` e depois `dismiss <vítima>` (mata agente alheio) ou `reassign <vítima> "<prompt malicioso>"` (sequestra). Era um **desvio direto do invariante do ADR-17** ("autoridade vem de estado que SÓ o host controla") — o `recruit` usava a linhagem host-only `_recruited_by` (correto), mas os outros 3 comandos foram esquecidos. **Decisão:** TODA decisão de autoridade sobre um nó-alvo usa **`_recruited_by` (linhagem host-only, infalsificável)** via o helper `_own_recruit(frm, nid)`; os cabos (`_maestro_connected`) ficam **só para exibição/UI** (o `list`), nunca para autoridade. `wire` só liga `frm` ou recrutas diretos de `frm` e **recusa** aresta que feche ciclo; `reassign` passa a **auditar**. **Concorrência (mesma revisão):** o `selectors` da stdlib NÃO é thread-safe — `add_node`/`remove_node` (main thread) mutavam o selector enquanto `serve()` fazia `select()` (thread daemon), sem lock (risco de `KeyError`/dict-changed derrubando o accept-loop). **Decisão:** só a thread do `serve` toca o selector; `add_node`/`remove_node` **enfileiram** a intenção (lista sob lock) e o `serve` drena antes do `select` (fecha F3 race + F4 unregister-faltando). Provas viraram testes da suíte (`test_confused_deputy_fechado`, `test_wire_recusa_ciclo_e_audita`, `test_add_node_substitui_sem_crashar`, `test_add_remove_concorrente_nao_derruba_o_serve`). Fontes (30/06/2026): OWASP Top 10 Agentic Apps 2026 (ASI03/08/10), CSA/SANS confused-deputy em agentes, arXiv:2605.05440 (authorization propagation multi-agente); selectors thread-safety (docs.python.org). Imutável (mudança = novo ADR).
- **ADR-19 (auto-aprovação do CLI interativo — flags por perfil, gated por nó) [2026-07-01]:** O terminal INTERATIVO do agente passa a poder rodar comandos SEM os prompts de permissão do CLI, quando o nó pede: **Maestro mode ligado** (Fase 1 — remove a interrupção a cada `maestri recruit`) OU o **toggle "permissão total"** por nó (Fase 2 — qualquer agente, on-demand). **Decisão/racional:** reafirma o **ADR-6** — "as flags de permissão do CLI só evitam prompts, NÃO confinam; o limite efetivo é o bwrap". Logo, remover os prompts **não afrouxa a segurança** (o bwrap + a autoridade-no-host do Maestro mode continuam); só tira o atrito. A supervisão em nível de fleet (HUD, auditoria, kill-switch, anomalia) permanece. **Flags VERIFICADAS nos binários instalados (2026-07-01):** claude **2.1.197** → `--permission-mode bypassPermissions` (circuit breakers p/ `rm -rf /`/`~` permanecem; alt.: `--dangerously-skip-permissions`; existe ainda o modo `auto` guardado se quisermos aprovar só o "seguro"); codex **0.142.4** → `--dangerously-bypass-approvals-and-sandbox` (o `--full-auto` foi REMOVIDO do interativo; a sandbox interna Landlock+seccomp do codex **aninhada no nosso bwrap quebra** — essa flag desliga a sandbox INTERNA dele, uso documentado "solely for environments that are externally sandboxed" = nosso bwrap). **Isolar o volátil (regra 5):** as flags ficam DECLARATIVAS no `[interactive].auto_approve` de cada `adapters/*.toml` (ADR-4) — trocar de versão/CLI não mexe no código; `agent_argv(auto_approve=)` as anexa. **Provado em runtime** (dentro do bwrap real, sem mock): claude e codex rodaram um `echo` sem prompt (rc=0). Persistência via `node_cfg` (maestro/autoapprove) — "abre igual fechou". Imutável (mudança = novo ADR).
- **ADR-20 (cabo interativo: HEADLESS por padrão; live vira opt-in de visibilidade) [2026-07-01]:** Revisa o DEFAULT do ADR-13. A resposta que o cabo (`maestro-ask`) entrega ao manager passa a vir por **headless** (mediado, resposta COMPLETA e limpa) por padrão; o modo **live** (digitar no terminal vivo do destino e RASPAR a tela) fica **opt-in** via `MAESTRO_ASK_MODE=live`, só para VISIBILIDADE. **Motivo (pesquisa ao vivo 2026-07-01, 2 frentes):** raspar TUI full-screen é frágil **por construção** — o próprio **Maestri real sofre o mesmo ~70%** (review agent-finder mai/2026; changelog dele reescrevendo o inter-agent CLI, health-monitoring, alt-screen, "custom PTY filters"). Causa-raiz: (a) TUIs desenham no **alternate screen buffer**, que **não vai para o scrollback** (claude-code#42670, docs de fullscreen); (b) PTY entrega em **chunks**; (c) a **detecção heurística de "terminou"** dispara no meio do stream. Consenso das boas práticas: *"a fonte-da-verdade da resposta nunca deve ser o pixel da tela; o live é telemetria, não transporte"* — usar canal estruturado/headless (`codex exec --json`, `claude -p --output-format json`; padrão A2A). **A objeção "headless perde contexto" é FALSA:** o `SessionManager.run_in_session` já mantém `session_id` por agente (1º turno `--session-id`, seguintes `--resume`), então o headless do cabo tem **contexto contínuo por destino** de graça. **Mudança mínima:** `_ask_mode` default `live`→`headless`; `_ask_delegate` só raspa se `mode=='live'`. Fallback headless preservado. `_ask_live`/`ask_capture` mantidos (para o opt-in). Fontes: pexpect FAQ ("Expect é frágil"), tui-use, arXiv 2505.02279 (A2A), Codex/Claude headless docs, themaestri.app changelog/connections, agent-finder.co (mai/2026). Imutável (mudança = novo ADR).
- **ADR-21 (autoridade NUNCA nasce de fiação visual — o lado da ESCRITA de `_recruited_by`; complementa ADR-17/18) [2026-07-02]:** Revisão adversarial pós-merge da Fase D (líder de grupo, v0.51.0) achou que `_materialize_team`, ao montar um grupo COM líder, fazia `self._recruited_by[colega] = líder` para desenhar a **caixa-preta visual** (líder = único ponto de conexão do grupo pra fora). Mas `_recruited_by` é a **mesma linhagem host-only** que `_own_recruit` lê para autorizar `dismiss`/`reassign`/`wire` (ADR-18) → o líder virava, sem querer, **recrutador-de-fato** dos colegas e ganhava **poder de comando sobre eles de graça** (escalada de privilégio; confirmado empiricamente — `_own_recruit(líder, colega)` retornava `True`), exatamente o poder que a Fase D dizia que o líder **não** teria. **Decisão:** separar **fiação visual** de **autoridade** — `self.edges.add(líder, colega)` continua (é o que dá a caixa-preta do grupo, só exibição/UI), mas `_recruited_by` **NÃO** é escrito para o líder; a autoridade permanece com quem já a tinha ANTES da Fase D (o `manager`, se houver; **ninguém**, se for materialização top-level via FAB/humano). Fix em `_materialize_team` (`maestro/native/canvas.py`); PR #52.
  - **O invariante que este ADR fecha (a novidade vs ADR-17/18):** o ADR-17 e o ADR-18 cobriam o lado da **LEITURA** — "autoridade LÊ só a linhagem host-only, nunca os cabos". Faltava o lado da **ESCRITA**: **o campo de linhagem `_recruited_by` só pode ser GRAVADO por um recrutamento genuíno autorizado pelo host — nunca como efeito colateral de layout/agrupamento/fiação visual.** Juntos: `_recruited_by` é sagrado nas duas pontas (lê-se só pra autoridade; escreve-se só em recrutamento real). *Lição de processo aplicada (`~/.claude/LESSONS.md` — "aplicar um invariante em TODAS as entradas"):* auditar **todo** ponto que ESCREVE `_recruited_by`, não só o `recruit` óbvio — a materialização de equipe era uma entrada esquecida, do mesmo jeito que `wire`/`dismiss`/`reassign` foram as esquecidas do ADR-18.
  - **Evidência externa (registrada com honestidade, pós-advogado-do-diabo):** esta classe de bug — **autoridade/comportamento emergindo de um detalhe incidental de construção em vez de um contrato explícito** — não é só nossa: o CrewAI (issue [#4783](https://github.com/crewAIInc/crewAI/issues/4783), fechada "not planned") sofre uma instância **parente**, porém **NÃO idêntica** — lá é *visibilidade/under-provision* (o manager hierárquico não enxerga os colegas em `self.agents` e faz tudo sozinho); aqui é *derivação-de-autoridade/over-grant* (o líder ganha poder que não devia). Registrar como **analogia direcional**, não como "mesma classe validada em produção" — a regra se sustenta pelo **nosso próprio** bug, medido no código; o paralelo externo só reforça que ferramentas maduras erram no mesmo eixo (control-plane vs execução). Ressalva incorporada da **revisão adversarial do relatório `docs/16` pelo Fable 5 (2026-07-02)**, que apontou o risco de superestimar esse paralelo.
  - **Testes (provas viraram suíte):** `test_team_materialize.py` — 2 testes corrigidos (autoridade esperada era o líder, virou `manager`/nenhuma) + `test_materialize_grupo_com_leader_lider_nao_ganha_autoridade_sobre_colegas` (regressão: `_own_recruit(líder, colega)` deve ser `False`; `_own_recruit(manager, colega)` deve ser `True`). Fontes: PR #52; ADR-17/18 (invariante-mãe); OWASP Top 10 Agentic Apps 2026 (ASI03 privilege); `docs/14` §12.5; `docs/16` (pesquisa de diferenciais + revisão Fable 5). Imutável (mudança = novo ADR).
- **ADR-22 (budget cap = contador MONOTÔNICO host-side; soft=aviso; hard barra no delegate) [2026-07-03]:** Implementa o controle que o ADR-17 (Etapa 3) exige — "budget por CUSTO REAL de tokens" — sobre o medidor F1 (v0.54.0). **Revisão adversarial (Fable 5) achou 3 furos na v1 do plano** (`docs/20`), corrigidos aqui: **(1) laundering:** somar o custo dos "agentes vivos" é contornável pelo runaway (o adversário do ADR-17) — ele **dispensa** o agente caro ou **rotaciona a sessão** (JSONL novo começa do zero) → o total cai → o cap "libera". **Decisão:** o gasto contado é um **acumulador monotônico no host** (`ui_state budget_fleet_total`) que **só soma deltas positivos** por agente (`record_spend`: se a sessão cresceu, `novo−último`; se rotacionou, banca o novo; **nunca subtrai**, inclui dispensados). Aplica a lição-mãe (`~/.claude/LESSONS.md`, ADR-17/18): *gasto contado não pode diminuir por ação do agente*. O humano zera pelo **baseline/reset** (botão só na UI do host — nunca comando de agente). **(2) kill-switch espúrio:** pós-hard TODO dispatch é bloqueado pra sempre; se `budget_blocked` contasse como abuso, uma cadeia com retry armaria o kill-switch global por **estado permanente** (não runaway). **Decisão:** `budget_blocked` fica **FORA de `ABUSE_EVENTS`** — vai pra trilha, não pro `spawn_anomaly`. **(3) soft=HITL falso:** custo é **monotônico** (cruzou o soft, todo turno fica acima pra sempre) → "pausa-e-confirma" por-turno vira um hard-cap com UX pior, ou trava um run autônomo esperando um humano ausente. **Decisão:** **soft = só AVISO** (notificação única + HUD âmbar/vermelho + top-spender), **só o hard barra**. Diferença fundamental do cap de recrutamento (contagem é reversível via dismiss; custo não é).
  - **Enforcement:** o hard barra no `Orchestrator.delegate` (fronteira robusta, cobre todo dispatch mediado) — recusa o turno (envelope `BLOCKED`) ANTES de rodar o agente; best-effort (erro no check não trava tudo). **Escopo GLOBAL** do fleet (é **disjuntor, não árbitro**: dinheiro é fungível, parar todo mundo quando a carteira estourou é o seguro; per-linhagem é justiça/atribuição, adiado). **Overshoot ACEITO e documentado:** medir pós-turno deixa passar até `N_paralelo × custo_de_turno` acima do hard (turno: centavos a ~$15 no patológico) — é disjuntor, não medidor de precisão; estimativa pré-turno foi preterida (chute). **Codex sem preço** só marca no HUD (não entra no $; sem teto de tokens no MVP).
  - **Provas viraram suíte:** `test_budget.py` — verdict, contador monotônico, **resistência a laundering** (rotação de sessão não baixa o contado), reset/baseline, e o **gate real do delegate** (agente NÃO roda quando estoura) + probe de runtime do HUD (âmbar/vermelho/reset). Fontes: `docs/20` (plano + revisão Fable 5); ADR-17 (invariante-mãe + Etapa 3 do budget); ADR-18/21 (autoridade = estado host-only, aplicado agora ao gasto contado). Imutável (mudança = novo ADR).
- **ADR-23 (unload de nó = SIGKILL direto; a "escalada graciosa" SIGTERM→SIGKILL é ILUSÃO dentro do bwrap) [2026-07-03]:** Story B do unload (`docs/21` §4-B) precisava decidir entre SIGKILL direto (espelhar `_kill_all_agents`) ou um 3º estado `"unloading"` com a escalada SIGTERM→1500ms→SIGKILL do respawn. **Revisão adversarial (Fable 5) rodou um spike de runtime nesta máquina** com os MESMOS flags de `maestro/engine/sandbox.py` (`--unshare-pid --die-with-parent --cap-drop ALL`): um `trap TERM` dentro do bwrap **nunca disparou** ao sinalizar o topo — o bwrap NÃO repassa SIGTERM ao filho; o namespace colapsa duro, como SIGKILL. **Consequência retroativa:** a escalada de `_respawn_node` **nunca entregou um SIGTERM ao CLI de agente** (só a nó bash puro, sem bwrap) — o timeout de 1500ms é teatro para nó-agente, desde sempre. **Decisão:** unload usa **SIGKILL direto** (um 3º estado compraria complexidade na parte mais delicada do canvas — histórico de bugs de coalescing/id órfão — em troca de nada); sem perda real, pois o claude grava o transcript **por evento** no JSONL (a conversa já está no disco antes do kill; resposta parcial em voo se perde igual nos dois sinais). **Fix anti-race obrigatório** (mesma revisão): antes do SIGKILL, zerar `_respawn_state`/`_respawn_pending` e cancelar `_respawn_force_src` — senão um respawn em voo faz `_on_child_exited` RESSUSCITAR o nó recém-descarregado (provado em teste: `test_race_respawn_em_voo_ressuscitaria_sem_o_fix`). **Guard de ociosidade:** confirmação **SEMPRE** (reforçada quando `tui_busy`) — `tui_busy` lê só a tela visível (falso negativo com scroll/prompt de permissão), então o falso negativo degrada pra "1 clique a mais", nunca pra kill silencioso; bloquear quando ocupado mataria o caso nº 1 de unload (agente travado comendo RAM). Qualquer respawn limpa a flag `unloaded` em `_do_respawn` (ponto único por onde os ~8 gatilhos passam). Riscos residuais aceitos e endereçados na story C: JSONL com última linha truncada / `tool_use` pendurado → o reload trata `--resume` falho com fallback explícito (avisar + spawn limpo); spike barato de kill-mid-tool-use antes de fechar a feature. Testes: `test_unload_node_canvas.py` (kill REAL de processo, race provada e fechada, flag, monitor, guard). Fontes: spike bwrap 2026-07-03 (Fable 5); `docs/21` (plano + captura A′). Imutável (mudança = novo ADR).
- **ADR-24 (fronteira com o runtime do agente e dependências externas — pertencem ao usuário, nunca embutidas/auto-atualizadas) [2026-07-04]:** Decisão derivada da pesquisa de dores da concorrência (`docs/22`-`24`), **revisada adversarialmente pelo Fable** (que cortou 3 "guardas" do rascunho por serem priorização volátil/lema/escopo de UI — foram pro `docs/10`, não viram ADR imutável). **(a)** O maestro-console SEMPRE usa o CLI de agente (claude/codex/opencode) que o **usuário instalou e versiona**; **nunca** auto-atualiza nem embute uma cópia própria — no máximo reporta a versão em diagnóstico. *Evidência:* Jean #460 (`docs/23`) — auto-update silencioso do CLI gerenciado quebrou o produto e invalidou o pin de versão do usuário; superset (cap. 28) baixa o próprio OpenCode ignorando o instalado. **(b)** Nenhuma dependência/serviço de terceiro **obrigatório** para funções **não-essenciais** (ex.: provedor de LLM embutido só pra nomear branch/commit ou enfeite). *Evidência:* dmux (cap. 32) tornou o OpenRouter obrigatório → revolta + forks da comunidade. *Racional:* o CLI e provedores externos são a **camada volátil** — ficam atrás da fronteira do usuário (reafirma ADR-6/ADR-10 "headless CLI é o caminho de dados" + regra global de stack-durável "isolar o volátil / minimizar lock-in"). *Alternativa rejeitada:* gerenciar/embutir o CLI "pra controlar a experiência" — troca a estabilidade do usuário pela conveniência do vendor, exatamente o anti-padrão medido. Fontes: `docs/23` (caps. 18, 28, 32), `docs/24` §4; revisão adversarial Fable 2026-07-04. Imutável (mudança = novo ADR).
- **ADR-25 (detecção de crash = dirty-flag no `ui_state` + handler `SIGTERM`/`SIGHUP`→quit; SEM marcador de PID) [2026-07-05]:** O reattach de nós órfãos (`docs/25`) precisava distinguir **fechamento limpo de crash** — sinal que NÃO existia (não havia atexit/pidfile/heartbeat; `_child_pid` é memória volátil). **Decisão:** sentinela *dirty-flag* — `ui_state["dirty_run"]` **armada no boot** (`crash_flag.check_and_arm`, logo após abrir o Store) e **limpa no shutdown limpo** (`crash_flag.disarm` no `on_shutdown`); se no próximo boot ainda está suja, o run anterior crashou. Durável porque o Store é **SQLite WAL** (a escrita commitada sobrevive a crash de processo; só power-loss do SO arrisca a última — que é o crash que se quer pegar de qualquer jeito). **Correção adversarial obrigatória (Fable):** o sinal `GApplication::shutdown` do GTK **NÃO dispara** em `SIGTERM`/`SIGHUP` (desligamento/logout do sistema, fechar a tampa do uConsole) — só em `quit()`/última janela fechada. Sem tratar esses sinais, **toda saída dessas deixaria a flag suja → todo boot marcaria "crash"**, degradando o "abre igual fechou". **Decisão:** `GLib.unix_signal_add(SIGTERM/SIGHUP → app.quit())` converte encerramento do sistema em shutdown limpo; sobra só `SIGKILL`/OOM/power-loss como "sujo" — exatamente o crash real. **Alternativa REJEITADA (Fable, `docs/25` §3.2):** marcador `pid:starttime` por nó pra checar processo sobrevivente — **descartado** porque `sandbox.py` usa `--unshare-pid --die-with-parent` (bwrap vira PID 1 do namespace; se o app morre, a árvore inteira morre) → no crash o filho-agente **sempre** morre, nunca há sobrevivente a proteger; e "estava trabalhando?" já é o JSONL no disco (`newest_session_id`). Cortar o marcador tira código da parte mais delicada do canvas (alinha com ADR-23). **Premissa aceita (decisão do usuário 2026-07-05):** "1 instância por vez" — o app roda `NON_UNIQUE`, então 2 instâncias no mesmo `state.db` colidiriam no flag global; sem `flock` (risco residual não-destrutivo: no pior caso um crash não detectado, nunca corrupção). **Verificação em runtime (2026-07-05, app GTK real, home isolado):** (A) boot arma `dirty_run=1`; `SIGTERM` → quit limpo → flag limpa; (B) `kill -9` (crash) mantém a flag suja → relaunch detecta o nó-agente com transcript no disco como órfão (`orphan=1`/`unloaded=1`/`session`), enquanto um agente sem transcript NÃO é marcado. Testes: `tests/test_crash_flag.py`, `tests/test_orphans.py` (gi-free) + `tests/test_reattach_canvas.py` (canvas real). Fontes: `docs/25` (plano + revisão Fable §10); man7 `proc_pid_stat(5)` (registro do `starttime`, não usado). Imutável (mudança = novo ADR).
- **ADR-27 (briefing por grupo = contexto host-only entregue por BLOCO DE ARQUIVO no workspace; nunca por prompt; agente nunca escreve) [2026-07-12]:** O brief do grupo (objetivo atual + decisões/contexto — `docs/30`) precisava chegar a TODO agente que nasce no grupo. **A via "injetar no 1º prompt" foi REPROVADA na revisão adversarial (Fable 5):** nos caminhos FAB e recruit **não existe 1º prompt** (o spawn é um CLI num VTE — `_spawn_into`); e injeção única em prompt DECAI em sessão longa ("rule amnesia" pós-compactação, documentada na comunidade Claude Code). **Decisão:** o brief viaja pelo MESMO trilho dos papéis — **bloco marcado (`maestro-brief:begin/end`) em CLAUDE.md/AGENTS.md do workspace isolado** (`engine/briefs.py`, espelho de `install_role_block`), que o CLI lê sozinho no start; headless lê de graça (cwd=workspace, a CADA run) e reattach/`--resume` relê no start — a "re-injeção" vem grátis. **Modelo de segurança (ADR-17 aplicado):** a fonte da verdade é o **Store** (chaves `ui_state` por gid, padrão ADR-22); o arquivo é ESPELHO descartável **re-carimbado a cada start** e **NUNCA lido de volta** (o workspace é gravável pelo agente — rabisco contamina só a si mesmo e morre no próximo carimbo); edição SÓ pela UI do host (nenhum comando `maestri` toca brief — mesmo princípio do "zerar budget só no host"); **sanitização no save** contra Unicode invisível (ataque "Rules File Backdoor", Pillar Security 2025 / CSA 2026: zero-width/bidi/tags escondem instrução de LLM em arquivo de regras); **cap de tamanho** (1000/80 chars) por evidência de degradação com contexto longo (Chroma "Context Rot", 2025) + guia oficial do CLAUDE.md ("concise"). **Pertença que importa é decisão HOST, nunca geometria** (reafirma ADR-21): grupo de nascimento gravado em `node_cfg birth_group` no GESTO (clique do FAB / manager do recruit / grupo criado pela equipe) — `_group_members` (sobreposição ≥25%) segue sendo SÓ desenho. **Riscos residuais aceitos e documentados** (`docs/30` §6): brief é amplificador do que o HUMANO colar (o dono não é o adversário — ADR-17); obsolescência (data da edição visível na UI e no bloco); auto-edição mid-session (corrige no próximo start); nó-shell sem workspace fica fora (mesma assimetria dos roles). *Alternativas rejeitadas:* fila/memória compartilhada mutável-por-agente estilo CrewAI/LangGraph (canal de contaminação que o ADR-17 proíbe); nota-conectada manual (existia e não resolve — exige lembrar de ligar por agente). Fontes: `docs/30` (plano + pesquisa com fontes/datas §3, revisão §9); vibe-kanban #3424 (a dor, verificada 2026-07-12). Imutável (mudança = novo ADR).
